<h2 class="page-header">Recently Added</h2>
<div>
  <% books.forEach(book => { %>
  <div>
    <a href="/books/<%= book.id %>"><img height="150px" src="<%= book.coverImageLink %>" alt="<%= book.title + " cover image" %>"></a>
  </div>
  <% }) %>
</div>